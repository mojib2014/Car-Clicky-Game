import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Car Clicky Game!</h1>
        <p>Click on an image to earn points, but don't one car twice!</p>
    </div>
);

export default Title;

